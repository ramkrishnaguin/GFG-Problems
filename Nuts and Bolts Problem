class Solution:

	def matchPairs(self, n, nuts, bolts):
		# code here
		a=['!','#','$','%','&','*','?','@','^']
		b=[]
		for i in range(len(a)):
		    if a[i] in nuts:
		        b.append(a[i])
		for i in range(len(nuts)):
		    nuts[i]=b[i]
		    bolts[i]=b[i]
