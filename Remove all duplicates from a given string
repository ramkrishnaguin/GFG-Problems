class Solution:

	
	def removeDuplicates(self,str):
	    # code here
	    result= ''
        i = 0
        while(i < len(str)):
            if str[i] in str:
                if str[i] not in result:
                    result += str[i]
            i += 1
        return (result)
